export default  {
    SET_LIST(state, list) {
        state.list = list;
    },
    SET_USER(state, userInfo) {
        state.userInfo = userInfo;
    },
    SET_ITEM(state, itemInfo) {
        state.itemInfo = itemInfo;
    },
};

// export { mutations };