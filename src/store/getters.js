export default {
    fetchedList(state) {
        return state.list;
    },
    fetchedUserInfo(state) {
        return state.userInfo;
    },
    fetchedItemInfo(state) {
        return state.itemInfo;
    },
};

// export { getters };